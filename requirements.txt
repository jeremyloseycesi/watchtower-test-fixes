# Security-patched versions
# Updated to address known CVEs

# CVE-2023-32681 - Proxy-Authorization header leak
requests==2.31.0

# CVE-2023-30861 - Cookie parsing vulnerability
flask==2.3.2

# CVE-2023-44271 - Arbitrary code execution via PIL.ImageMath.eval
pillow==8.1.1

# CVE-2020-14343 - Arbitrary code execution via yaml.load
pyyaml==5.4

# Additional packages with security fixes
urllib3==1.26.5

# Jinja2 upgraded for Flask 2.3.2 compatibility (requires jinja2>=3.0)
jinja2==3.0.3

# Additional Flask dependencies
werkzeug>=2.3.0
click>=8.0.0
itsdangerous>=2.0.0
markupsafe>=2.0.0