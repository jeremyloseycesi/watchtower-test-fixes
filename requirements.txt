# Intentionally OLD VERSIONS with known CVEs
# DO NOT USE IN PRODUCTION!

# CVE-2023-32681 - Proxy-Authorization header leak
requests==2.25.0

# CVE-2023-30861 - Cookie parsing vulnerability
# Flask 2.3.2 requires updated dependencies
flask==2.3.2
werkzeug==2.3.6
jinja2==3.1.2
itsdangerous==2.1.2
click==8.1.3
blinker==1.6.2

# CVE-2023-44271 - Arbitrary code execution via PIL.ImageMath.eval
pillow==8.1.1

# CVE-2020-14343 - Arbitrary code execution via yaml.load
pyyaml==5.4

# Additional vulnerable packages
urllib3==1.26.5
markupsafe==2.1.3